import SwiftUI

struct SettingsPage: View {
    var body: some View {
        VStack {
            Spacer()
            Text("Settings Page")
                .foregroundColor(.white)
                .font(.title)
            Spacer()
        }
    }
}
